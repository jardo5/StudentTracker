<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentTracker"
        tools:targetApi="31">
        <activity
            android:name=".utility.ClassDetails"
            android:exported="false"
            android:parentActivityName=".utility.TermDetails"
            android:label="Classes"/>
        <activity
            android:name=".utility.TermDetails"
            android:exported="false"
            android:label="Term Details and Class List"
            android:parentActivityName=".utility.TermList" />
        <activity
            android:name=".utility.TermList"
            android:exported="false"
            android:label="Terms"
            android:parentActivityName=".utility.MainActivity" />
        <activity
            android:name=".utility.MainActivity"
            android:exported="true"
            android:label="Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>